package components.database;

import haxe.ui.containers.Panel;

@:build(haxe.ui.ComponentBuilder.build('../../assets/main/database/database-list.xml'))
class DatabaseList extends Panel {
  public function new() {
    super();
  }
}
